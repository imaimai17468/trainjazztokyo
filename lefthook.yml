pre-commit:
  commands:
    check:
      glob: "*.{ts,tsx,js,jsx}"
      run: pnpm oxfmt --write {staged_files} && git add {staged_files} && pnpm oxlint {staged_files}

pre-push:
  commands:
    typecheck:
      run: pnpm typecheck
